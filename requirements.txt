# --- API server ---
fastapi==0.115.5
uvicorn[standard]==0.31.1
pydantic==2.9.2
python-dotenv==1.0.1
gunicorn
pytz

# --- AWS ---
boto3==1.40.71

# --- ML runtime (Corrected to match ML environment) ---
numpy==2.2.6             # MATCH ML ENV 
tensorflow==2.20.0       # MATCH ML ENV 
keras==3.12.0            # Explicitly add Keras version 
scikit-learn==1.7.2      # MATCH ML ENV (Crucial for model loading) 
joblib==1.5.2            # MATCH ML ENV 
plotly==6.4.0            
pandas==2.3.3            # MATCH ML ENV 

# --- Hidden Dependencies (To prevent ecosystem conflicts) ---
ml_dtypes==0.5.3         # Pinned for TensorFlow ecosystem stability 
protobuf==6.33.0         # Pinned for TensorFlow ecosystem stability